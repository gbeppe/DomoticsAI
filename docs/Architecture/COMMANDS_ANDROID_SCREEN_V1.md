# Android Commands Screen v1

La schermata `Comandi` legge `GET /api/v1/commands` e mostra il registro operativo del Command Manager. Il refresh avviene ogni tre secondi; SSE realtime verrà aggiunto nel prossimo incremento.
