# Flow originali

Conservare qui copie esportate e sanitizzate dei flow Node-RED di produzione.

Non inserire `flows_cred.json` o credenziali.
I file presenti in questa cartella sono solo riferimenti e non devono essere importati in produzione senza verifica.
